/** All doorstep deliveries cost this flat fee (GHS), regardless of location within Ghana. Store pickup is free. */
export const FLAT_DELIVERY_FEE = 40;

/** International shipping — flat, USD-denominated, converted to the order's actual currency at checkout. */
export const FLAT_INTERNATIONAL_DELIVERY_FEE_USD = 8;

/** Smallest order total we'll accept payment for, EUR-denominated, converted to the order's actual currency at checkout. */
export const MIN_ORDER_TOTAL_EUR = 20;
